# Quick Start Guide для langchain-xbaza

## 📦 Структура пакета

```
langchain_xbaza/
├── langchain_xbaza/          # Основной пакет
│   ├── __init__.py           # Экспорты
│   └── tools.py              # Реализация tools
├── tests/                    # Тесты
│   ├── __init__.py
│   └── test_tools.py
├── examples/                 # Примеры использования
│   └── basic_usage.py
├── .github/workflows/        # CI/CD
│   └── ci.yml
├── pyproject.toml           # Конфигурация проекта
├── setup.py                 # Setup скрипт
├── README.md                # Документация
├── LICENSE                  # Лицензия
├── CHANGELOG.md             # История изменений
├── PUBLISH.md               # Инструкции по публикации
└── requirements.txt         # Зависимости
```

## 🚀 Быстрый старт

### 1. Установка для разработки

```bash
cd langchain_xbaza
pip install -r requirements-dev.txt
pip install -e .
```

### 2. Запуск тестов

```bash
pytest tests/ -v
```

### 3. Проверка кода

```bash
# Форматирование
black langchain_xbaza/ tests/

# Линтинг
ruff check langchain_xbaza/ tests/
```

### 4. Сборка пакета

```bash
python -m build
```

### 5. Публикация

См. [PUBLISH.md](./PUBLISH.md) для подробных инструкций.

## 📝 Использование

```python
from langchain_xbaza import XbazaJobsTool

tool = XbazaJobsTool()
result = tool.run(category="IT", city="Minsk", limit=10)
print(result)
```

## 🔗 Следующие шаги

1. **Создать репозиторий на GitHub:**
   - Создать новый репозиторий `langchain-xbaza`
   - Скопировать содержимое директории `langchain_xbaza/`
   - Закоммитить и запушить

2. **Обновить ссылки:**
   - Заменить `yourusername` на реальный username в:
     - `pyproject.toml`
     - `README.md`
     - `setup.py`

3. **Протестировать локально:**
   ```bash
   pip install -e .
   python examples/basic_usage.py
   ```

4. **Опубликовать на PyPI:**
   - Следовать инструкциям в `PUBLISH.md`
   - Сначала на TestPyPI, затем на PyPI

5. **Добавить в документацию Xbaza:**
   - Обновить README.md основного репозитория
   - Добавить ссылку на пакет

## ✅ Чеклист перед публикацией

- [ ] Все тесты проходят
- [ ] Код отформатирован (black)
- [ ] Линтинг пройден (ruff)
- [ ] Обновлены все ссылки (yourusername → реальный username)
- [ ] README.md заполнен
- [ ] CHANGELOG.md обновлен
- [ ] Версия обновлена в pyproject.toml и __init__.py
- [ ] Пакет собран и проверен (twine check)
- [ ] Протестирован на TestPyPI

## 📞 Поддержка

- **Email:** ai@xbaza.by
- **GitHub Issues:** https://github.com/yourusername/langchain-xbaza/issues
- **API Documentation:** https://github.com/yourusername/xbaza-ai-api

